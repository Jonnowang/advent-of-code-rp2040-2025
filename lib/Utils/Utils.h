#ifndef UTILS_H
#define UTILS_H

// Comment out to use Serial output instead of FIFO
#define USE_FIFO_OUTPUT

void print_and_send_solution(int day, int part, long long result, unsigned long duration);

#endif // UTILS_H
